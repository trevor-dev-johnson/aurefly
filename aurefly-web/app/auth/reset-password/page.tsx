import { ResetPasswordClient } from "./reset-password-client";

export const metadata = {
  title: "Choose New Password | Aurefly",
};

export default function ResetPasswordPage() {
  return <ResetPasswordClient />;
}
